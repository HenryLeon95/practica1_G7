public class OperacionesBasicas{
    
    public static boolean esPrimo(int a, int b, int numero){
        int contador = 2;
        boolean primo=true;
	    numero = numero + a + b;
        while ((primo) && (contador!=numero)){
            if (numero % contador == 0)
            primo = false;
            contador++;
        }
        return primo;
    }

   Public int fnFibonacci(int n)
    {
        if (n>1 && n<100){
        return fibonacci(n-1) + fibonacci(n-2);  
        }
        else if (n==1) {  
            return 1;
        }
        else if (n==0){  
            return 0;
        }
        else{ //error
        if (n>=100){
            System.out.println("Debes ingresar un numero menor a 100");
            
        }
        else {
            System.out.println("Debes ingresar un numero mayor o igual a 1");
            return -1;
        } 
        }
    } 

    public int AreaTriangulo(int base, int altura){
        System.out.println("Los parametros enviados son:");
        System.out.println("Base:"+base);
        System.out.println("Altura:"+altura);
        
        int area= (base*altura)/2;
        System.out.println("El area es:"+area);
        return area;
    }

    public boolean VerificarPar(int numero){
        if(numero % 2 == 0){
            System.out.println("El número ingresado es PAR");
            return true;
        }
        else {
            return false;
        }
    }

    public int SumatoriaSucesiva(int numero)
    {
        int sumatoria = 0;
        int cont = 0;

        while (cont <= numero) {
            sumatoria = sumatoria + cont;
            cont++;
        }

        return sumatoria;
    }   
}

